{
  "name": "vistamark-ria-intel",
  "version": "0.17.1",
  "private": true,
  "description": "Vistamark Intel — Form ADV + IRS EO BMF + ProPublica + DOL Form 5500 + IPEDS endowments + NACUBO benchmarks + SBA PPP loans + USAspending federal awards + SEC Form 13F holdings + FDIC bank financials + OFR systemic-risk/funding + SEC EDGAR company intel (full-text search, Form D & cross-market XBRL frames) + GLEIF LEI entity resolution + BDC (listed business development companies) + PPD public pensions + FRED/Treasury macro with curated market-signals layer + Daily Treasury Statement (daily TGA cash + fiscal flows incl. withheld-tax payroll signal) + one-call morning market brief (GET /api/market-brief for cron), normalized Postgres + live APIs, MCP server for Randall.",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "vercel dev",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx scripts/migrate.ts",
    "seed": "tsx scripts/seed.ts",
    "ingest": "tsx scripts/ingest.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "adm-zip": "^0.5.16",
    "drizzle-orm": "^0.36.4",
    "fast-xml-parser": "^4.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.7",
    "@types/node": "^22.10.2",
    "@vercel/node": "^3.2.29",
    "drizzle-kit": "^0.30.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": "22.x"
  }
}
