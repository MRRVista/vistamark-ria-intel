{
  "name": "vistamark-ria-intel",
  "version": "0.1.0",
  "private": true,
  "description": "Vistamark RIA intelligence — Form ADV ingestion, normalized RIA database, MCP server for Randall.",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "vercel dev",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx scripts/migrate.ts",
    "seed": "tsx scripts/seed.ts",
    "ingest": "tsx scripts/ingest.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "drizzle-orm": "^0.36.4",
    "fast-xml-parser": "^4.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@vercel/node": "^3.2.29",
    "drizzle-kit": "^0.30.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
